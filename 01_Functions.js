//Declarative Function
//We can assign the values before the function.

student("Winter", 18, 01);

function student(name, age, rank) {
  console.log(
    name +
      " is " +
      age +
      " year old and his/her rank in the class is " +
      rank +
      "."
  );
}
